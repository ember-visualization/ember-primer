import curveLookup from 'dummy/utils/curve-lookup';
import { module, test } from 'qunit';

module('Unit | Utility | curve lookup');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = curveLookup();
  assert.ok(result);
});
