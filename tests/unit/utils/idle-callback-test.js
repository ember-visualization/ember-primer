import idleCallback from 'dummy/utils/idle-callback';
import { module, test } from 'qunit';

module('Unit | Utility | idle-callback', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = idleCallback();
    assert.ok(result);
  });
});
