import scaleHelperOptions from 'dummy/utils/scale-helper-options';
import { module, test } from 'qunit';

module('Unit | Utility | scale helper options');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = scaleHelperOptions();
  assert.ok(result);
});
